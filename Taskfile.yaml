# https://taskfile.dev

version: '3'

tasks:
  default:
    cmds:
      - mkdir -p bin
      - go build -o bin/mactools mactools.go 

  run:
    cmds:
      - ./bin/mactools
  
  test:
    cmds:
      - go test -v ./...


  
